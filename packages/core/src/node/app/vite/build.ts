import { UserConfig as ViteConfig } from 'vite';

import type { App } from '../App';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function build(app: App, config?: ViteConfig): Promise<void> {
  // ...
}
